using MiniCooked;
using System.Drawing;

public class ImageBox : RectUI
{
    public override void Print()
    {

    }

    //public ImageBox(int m_boxWidth, int m_boxHeight)
    //{
    //    this._boxSize.EndX = m_boxWidth;
    //    this._boxSize.EndY = m_boxHeight;
    //}

    //public ImageBox(Rect m_size)
    //{
    //    if(m_size.StartX != 0 || m_size.StartY != 0)
    //        throw new Exception("Rect 객체 잘못된 생성 Width, Height 값을 통해 생성자 호출요망");

    //    this._boxSize = m_size;
    //}

    //public ImageBox SetPos(int m_posX , int m_posY)
    //{
    //    _boxSize.EndX = _boxSize.EndX - _boxSize.StartX + m_posX;
    //    _boxSize.EndY = _boxSize.EndY - _boxSize.StartY + m_posY;

    //    _boxSize.StartX = m_posX;
    //    _boxSize.StartY = m_posY;

    //    return this;
    //}

    //public ImageBox SetPos() // 
}

