public enum HorizonAlign
{
    Left,
    Center,
    Right,
}

public enum VerticalAlign
{
    Top,
    Center,
    Bottom,
}

public enum RectOption
{
    Relative,
    Absolute,
}

public enum RectCorner
{
    TopL,
    TopR,
    BotL,
    BotR,
}
